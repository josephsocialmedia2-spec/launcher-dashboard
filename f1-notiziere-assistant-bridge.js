(()=>{'use strict';let timer=null;async function apply(){const box=document.querySelector('#f1AssistantPanel .f1-assistant-actions');if(!box||!window.F1NotiziereEngine?.ready?.())return false;try{const state=await F1NotiziereEngine.load(),i=state.instruction||{};box.innerHTML=`<button class="primary" data-f1-assistant="now"><i class="fa-solid fa-bolt"></i> COSA FACCIO ADESSO?</button><a id="f1AssistantExecute" href="${String(i.href||'ricerca-territoriale.html').replace(/"/g,'&quot;')}"><i class="fa-solid fa-play"></i> ${String(i.cta||'ESEGUI ORA').replace(/[<>]/g,'')}</a><button data-f1-assistant="explain"><i class="fa-solid fa-circle-info"></i> SPIEGA QUESTA SCHERMATA</button>`;return true}catch(e){console.warn('Assistente Notiziere',e);return false}}
function start(){let n=0;timer=setInterval(async()=>{n++;if(await apply()||n>100)clearInterval(timer)},100);window.addEventListener('f1:notiziere-state',apply)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();