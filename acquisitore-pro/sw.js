const CACHE='acquisitore-pro-core-v20';
const CORE=['./index.html','./manifest.webmanifest','./icon-192.svg','./icon-512.svg'];
self.addEventListener('install',event=>{event.waitUntil((async()=>{const cache=await caches.open(CACHE);await Promise.allSettled(CORE.map(url=>cache.add(new Request(url,{cache:'reload'}))));await self.skipWaiting();})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('acquisitore-pro-')&&k!==CACHE).map(k=>caches.delete(k)));await self.clients.claim();})())});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;const isNav=event.request.mode==='navigate';if(isNav){event.respondWith((async()=>{const cached=await caches.match('./index.html');if(cached){event.waitUntil(fetch('./index.html',{cache:'no-store'}).then(async r=>{if(r.ok)(await caches.open(CACHE)).put('./index.html',r.clone())}).catch(()=>{}));return cached}try{const r=await fetch(event.request);if(r.ok)(await caches.open(CACHE)).put('./index.html',r.clone());return r}catch(e){return new Response('<!doctype html><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Acquisitore Pro</title><body style="font-family:Arial;padding:24px"><h2>Acquisitore Pro</h2><p>App temporaneamente offline. Riprova tra pochi secondi.</p></body>',{headers:{'content-type':'text/html; charset=utf-8'}})}})());return}if(CORE.some(x=>url.pathname.endsWith(x.slice(1)))){event.respondWith(caches.match(event.request).then(cached=>cached||fetch(event.request).then(async r=>{if(r.ok)(await caches.open(CACHE)).put(event.request,r.clone());return r})));return}
// Funzioni secondarie: rete normale, senza bloccare l'avvio e senza cache aggressive.
event.respondWith(fetch(event.request).catch(()=>caches.match(event.request)));
});